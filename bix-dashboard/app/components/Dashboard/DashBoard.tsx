import styled from "styled-components";

const Container = styled.div`
padding: 20px;
`;

const DashBoard = () => {
  return (
    <Container>
      <h1>Dashboard</h1>
    </Container>
  );
};

export default DashBoard;
